# ./wolff.sh
```bash
cat <<< 'big shell scripting fan here' | tee greetings
__=$_ && awk '{ sub(/.*/, "etis ym ot emoclew"); print }' < $__ | rev
printf "note: I'm a tad extra. i can't help it" | perl -nle "print $& if m{.+}"
echo $(r 2>/dev/null) | grep -Eo "[eihmr]" | head -n6 | tr -d '\n' | rev | sed 's/re/& /'
```
